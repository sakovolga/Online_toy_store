spring:
  main:
    banner-mode: off
  liquibase:
      enabled: false

  datasource:
      url: jdbc:h2:file:./testDB;AUTO_SERVER=TRUE
      username: sa
      password:
      driver-class-name: org.h2.Driver

  jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      hibernate:
          ddl-auto: none

  sql:
    init:
        mode: never


  h2:
    console:
      enabled: true

jwt:
  secret: a5f11ec8520dc165b59e4e2da5c848aff4a292cfe7f7b7c86d268532f2c3e088
  lifetime: 30m